/**
 * @fileoverview Firestore Security Rules for MathQuest application.
 *
 * WARNING: These rules are configured to be completely open for any authenticated user.
 * Any user who is signed in can read, write, and delete any data in this database.
 * This is intended for initial development and debugging ONLY.
 * Before deploying to production, these rules MUST be replaced with more restrictive,
 * secure rules to protect user data.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow read and write access to all documents for any signed-in user.
      allow read, write: if request.auth != null;
    }
  }
}
